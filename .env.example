# Meta: path to directory with real secrets (.env, credentials.json, members.json)
SENSITIVE_DIR=/home/bladvaran/ippon-secrets

# Meta: path to runtime config JSON (optional, defaults to ./resources/config/config.json)
CONFIG_PATH=./resources/config/config.json
